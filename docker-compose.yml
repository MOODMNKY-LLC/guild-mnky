services:
  discord-bot:
    build:
      context: ./discord-bot
      dockerfile: Dockerfile
    env_file:
      - .env.bot
    restart: unless-stopped
    container_name: guild-mnky-discord-bot
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
